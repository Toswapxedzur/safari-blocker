// Custom Web Blocker — example templates.
//
// Intentionally empty for now. The curated example set is regenerated from the
// engine source during the final replenish run.

(function () {
  CB_REGISTER_TEMPLATES([]);
})();
